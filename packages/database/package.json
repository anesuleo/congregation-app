{
  "name": "@congregation-app/database",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "typecheck": "echo \"(database) no TypeScript sources to check\""
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  }
}
